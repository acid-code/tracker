/** Android status-bar glyph + shade icon for Web Notifications. */
export function notificationVisuals(origin: string) {
  return {
    icon: `${origin}/icons/icon-192.png`,
    badge: `${origin}/icons/notification-badge-v2.png`,
  };
}
